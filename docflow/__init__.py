"""DocFlow — комплектование и приведение в порядок проектной документации.

Пакет содержит ядро программы (без GUI):
    config    — настройки приложения (пути, опции), хранятся в config.json
    registry  — чтение «эталонного» реестра наименований из xlsx
    naming    — распознавание и формирование имён файлов по реестру
    scanner   — инвентаризация папок и отслеживание изменений (снимок + diff)
    planner   — формирование плана действий (переименовать/архив/!LATEST)
    executor  — выполнение действий с журналом и откатом
"""

__version__ = "1.12.4"
__build_date__ = "2026-08-24"
